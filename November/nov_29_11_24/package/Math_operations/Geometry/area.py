from math import pi

def circle_area(radius):
    print(f"Area of circle(radius:{radius}):{pi * radius * radius}")

def triangle_area(base,height):
    print(f"Area of triangle(base={base},height={height}):{0.5*base*height}")